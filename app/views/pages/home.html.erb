<div class="w-full h-full mx-auto bg-white border border-gray-300 rounded-lg p-4 md:p-8 shadow-sm">
    <div class="mb-4">
        <h1 class="text-3xl font-bold mb-4">Quote editor</h1>
        <p>A blazing fast quote editor built with Hotwire</p>
    </div>
    
    <div>
        <% if user_signed_in? %>
            <%= link_to "View quotes", quotes_path, class: "btn btn--dark" %>
        <% else %>
            <%= link_to "Sign in", new_user_session_path, class: "btn btn--dark" %>
        <% end %>
    </div>
</div>